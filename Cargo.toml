[package]
name = "offi-icu"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Proof-of-concept Magnus extension with runtime-loaded ICU4C formatters"
repository = "https://github.com/erickguan/offi-icu"

[lib]
name = "offi_icu"
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
libloading = "0.8"
magnus = "0.8.2"
thiserror = "2"

[dev-dependencies]
